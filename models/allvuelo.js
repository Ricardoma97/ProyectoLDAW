/*const mongoose= require('mongoose');
const vuelo= new mongoose.Schema({
	_id: mongoose.Schema.Types.ObjectId,
	destino: String,
	tipoDeVuelo: String,
	date:Date,
	capacidad:Number,
	piloto:{//Piloto
		id_referencia: ObjectId,
		photo:String,//o img
		Name:String,
		Birthday:Date,
		Direccion:String,
		mail:String,
		numeberOfFlights:Number
	}

})

const AllvuelosSchema = new mongoose.Schema({
	_id: mongoose.Schema.Types.ObjectId,
	vuelos:[vuelo]
});

module.exports = mongoose.model('allVuelos', UserSchema);*/